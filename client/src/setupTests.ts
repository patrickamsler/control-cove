// jest-dom adds custom matchers for asserting on DOM nodes; the /vitest entry
// registers them with Vitest's expect. Allows you to do things like:
// expect(element).toHaveTextContent(/react/i)
// learn more: https://github.com/testing-library/jest-dom
import '@testing-library/jest-dom/vitest';
